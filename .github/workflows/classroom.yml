name: Autograding Tests
'on':
- push
- repository_dispatch
permissions:
  checks: write
  actions: read
  contents: read
jobs:
  run-autograding-tests:
    runs-on: ubuntu-latest
    if: github.actor != 'github-classroom[bot]'
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - name: Check for `SQD lab.ipynb`
      id: check-for-sqd-lab-ipynb
      uses: classroom-resources/autograding-command-grader@v1
      with:
        test-name: Check for `SQD lab.ipynb`
        setup-command: ''
        command: test -f SQD lab.ipynb
        timeout: 1
        max-score: 10
    - name: Autograding Reporter
      uses: classroom-resources/autograding-grading-reporter@v1
      env:
        CHECK-FOR-SQD-LAB-IPYNB_RESULTS: "${{steps.check-for-sqd-lab-ipynb.outputs.result}}"
      with:
        runners: check-for-sqd-lab-ipynb
